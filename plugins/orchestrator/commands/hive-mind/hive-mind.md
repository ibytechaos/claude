# hive-mind

Hive Mind collective intelligence system for advanced swarm coordination.

## Usage
```bash
orchestrator hive-mind [subcommand] [options]
```

## Subcommands
- `init` - Initialize hive mind system
- `spawn` - Spawn hive mind swarm
- `status` - Show hive mind status
- `resume` - Resume paused session
- `stop` - Stop running session

## Examples
```bash
# Initialize hive mind
orchestrator hive-mind init

# Spawn swarm
orchestrator hive-mind spawn "Build microservices"

# Check status
orchestrator hive-mind status
```
